import "package:flutter/material.dart";

class CT_Scan extends StatelessWidget {
  static const screenRoute = '/CT_Scan';
  const CT_Scan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
